- Adding GPU passthrough to LXC for Immich (Q: does this stop another VM from using the GPU? the *arr stack + Jellyfin setup might need it).

- Move qBittorrent + Gluetun into the *arr stack VM (it's easier if they co-exist)
