services:
  runner:
    build: .
    network_mode: "host"
    volumes:
      - .:/infrastructure
    environment:
      - ANSIBLE_CONFIG=/infrastructure/ansible/ansible.cfg
    stdin_open: true
    tty: true
