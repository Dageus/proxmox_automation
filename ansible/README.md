# Ansible

## Debugging

### Seeing the inventory in a graph

```
ansible-inventory -i inventories/proxmox.yaml --graph
```

### Seeing the inventory vars

```
ansible-inventory -i inventories/proxmox.yaml --list
```
