---
- name: Initialize Proxmox Cluster
  hosts: proxmox_nodes
  vars_files:
    - ../../shared_vars.yaml
  roles:
    - role: proxmox_init
      vars:
        pve_remove_local_lvm: true
        pve_create_root_api_token: true
        pve_configure_ups: false
